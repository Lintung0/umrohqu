Lintang Mahameru Putra || SMKN 6 Malang

Laporan Mingguan: 20 Juli - 26 Juli 2026

Projek: UmrohQ Marketplace Haji & Umroh

=> Membuat halaman Homepage dengan Hero, featured packages, travel terpercaya, promo, artikel, dan FAQ ✅
=> Membuat halaman `/search` dengan filter lengkap (tanggal, kota, maskapai, hotel, harga) ✅
=> Membuat halaman `/compare` untuk perbandingan paket umroh ✅
=> Membuat halaman `/travel` (listing travel) dan `/travel/[id]` (detail travel dengan profil, paket, kontak) ✅
=> Membuat halaman `/package/[slug]` untuk detail paket umroh lengkap (itinerary, fasilitas, review, pemesanan) ✅
=> Membuat halaman `/checkout` 2-step (data jamaah → pembayaran) dengan 8 metode pembayaran dan promo code ✅
=> Membuat halaman `/articles` dan `/articles/[slug]` untuk blog/artikel umroh ✅
=> Membuat halaman `/promotions` untuk daftar promo aktif ✅
=> Membuat halaman `/faq`, `/about`, `/terms`, `/privacy` ✅
=> Membuat halaman 404 Not Found ✅
=> Membuat Navbar responsive dengan mobile menu dan auth-aware (avatar + dropdown saat login) ✅
=> Membuat Footer dengan link yang valid ke semua rute ✅
=> Membuat halaman login, register, forgot password, reset password, verify email, dan complete profile ✅
=> Membuat integrasi Google OAuth untuk login via Google ✅
=> Membuat Supabase client (browser + server) dan auth trigger SQL untuk auto-create user row ✅
=> Membuat layout dashboard customer dengan sidebar navigasi ✅
=> Membuat halaman Overview dashboard customer ✅
=> Membuat halaman `/dashboard/bookings` (daftar pesanan) dan `/dashboard/bookings/[id]` (detail pesanan) ✅
=> Membuat halaman `/dashboard/wishlist` untuk paket favorit ✅
=> Membuat halaman `/dashboard/settings` untuk profil dan pengaturan akun ✅
=> Membuat layout dashboard travel dengan sidebar navigasi ✅
=> Membuat halaman Overview travel dengan stats (paket aktif, booking, revenue, jamaah) dan chart ✅
=> Membuat halaman `/travel-dashboard/packages` untuk CRUD paket umroh (grid view, search, filter) ✅
=> Membuat halaman `/travel-dashboard/bookings` untuk manajemen pesanan (tabel, filter status, konfirmasi/batalkan) ✅
=> Membuat halaman `/travel-dashboard/pilgrims` untuk data jamaah (tabel dengan paspor, kontak, status) ✅
=> Membuat halaman `/travel-dashboard/reports` untuk laporan penjualan (revenue chart, channel breakdown) ✅
=> Membuat halaman `/travel-dashboard/website` untuk pengaturan subdomain, branding, logo, dan media sosial ✅
=> Membuat halaman `/travel-dashboard/promotions` dengan tab Promo dan tab Bidding (CTR, impressions) ✅
=> Membuat halaman `/travel-dashboard/settings` dengan 4 tab (Profil, Password, Notifikasi, Billing) ✅
=> Membuat halaman `/admin` Overview dengan stats platform, revenue chart, dan quick actions ✅
=> Membuat halaman `/admin/travels` untuk manajemen akun travel (verifikasi, suspend, reject, card view) ✅
=> Membuat halaman `/admin/templates` untuk manajemen template website (preview, kategori, status) ✅
=> Membuat halaman `/admin/config` untuk konfigurasi biaya (setup fee, service fee, payout, pajak) ✅
=> Membuat halaman `/admin/bidding` untuk manajemen bidding (posisi, impressions, CTR, periode) ✅
=> Membuat halaman `/admin/promos` untuk promo global dan promo per travel ✅
=> Membuat halaman `/admin/reports` untuk laporan sistem (revenue platform, top travel) ✅
=> Membuat halaman `/admin/onboarding` untuk visualisasi alur onboarding mitra ✅
=> Membuat halaman `/admin/verification` untuk verifikasi dokumen travel (approve/reject) ✅
=> Membuat halaman `/admin/tickets` untuk tiket kendala (prioritas, status, kategori) ✅
=> Membuat halaman `/admin/help` untuk pusat bantuan (FAQ, kontak support) ✅
=> Membuat halaman `/admin/setup-fees` untuk manajemen biaya setup travel ✅
=> Membuat halaman `/admin/service-fees` untuk konfigurasi service fee (% + flat) ✅
=> Membuat halaman `/admin/invoices` untuk manajemen invoice (pending/paid/overdue) ✅
=> Membuat halaman `/admin/payments` untuk payout history ke travel ✅
=> Membuat halaman `/admin/billing-promos` untuk mekanisme diskon ✅
=> Membuat halaman `/admin/billing-reports` untuk laporan keuangan UmrohQ ✅
=> Membuat AdminSidebar dengan role switcher (dropdown untuk ganti role Admin/Support/Billing) ✅
=> Membuat modul Fee Engine untuk kalkulasi biaya per-orang (Rp300k portal, Rp100k subdomain) × jumlah jamaah ✅
=> Membuat modul Bidding & Ranking dengan weighted scoring (Bid 40% + Rating 25% + Performance 20% + Trust 15%) ✅
=> Membuat modul Revenue Engine untuk revenue summary, per-travel breakdown, dan monthly aggregation ✅
=> Membuat Service Abstraction Layer (DataProvider interface, InMemoryProvider, SupabaseProvider placeholder) ✅
=> Memperbarui halaman Checkout untuk menggunakan Fee Engine dengan channel selector (Portal vs Subdomain) ✅
=> Fix environment variable NEXT_PUBLIC_SUPABASE_URL dan konfigurasi image remotePatterns ✅
=> Menambahkan field `isActive` ke UmrohPackage type untuk fitur aktif/nonaktif paket ✅

Link Video: https://youtu.be/dz5NhyzupHM
